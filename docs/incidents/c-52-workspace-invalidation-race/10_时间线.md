# 时间线

## 关键事实时间线

- `2026-05-15 11:15:53 +08:00`
  Symphony 为 `C-52` 启动一次顶层 `agent run` 与一次 `worker attempt`，并创建 workspace。

- `2026-05-15 11:16:04 +08:00`
  顶层主会话 `019e29a2-51c7-7813-866d-e75c1094bf83` 建立。
  该会话 `originator=symphony-orchestrator`，`cwd=/home/ss/data/projects/symphony-workspaces/C-52`。

- `2026-05-15 11:25:48 +08:00` 到 `11:44:05 +08:00`
  同一条顶层 run 顺序完成 `turn=1/20` 到 `turn=7/20`。
  在已核对材料范围内，没有看到第二次 `C-52` 顶层 `Starting agent run`。

- `2026-05-15 11:34:39 +08:00`
  顶层主会话派生实现子代理 `Boyle`。
  `Boyle` 是 `parent_thread_id=019e29a2-...` 的 subagent，不是第二个顶层 worker。

- `2026-05-15 11:44:12 +08:00`
  顶层主线程收到 continuation guidance，进入 `turn #8 of 20`。
  这说明 cleanup 发生前，这条线程仍在活跃推进，而不是早已失效的死会话。

- `2026-05-15 11:47:14 +08:00` 到 `11:47:50 +08:00`
  同一顶层会话继续执行 `commit`、准备 PR body、`git push`。
  也就是说，cleanup 发生前它仍在正常工作流内推进实现与收口。

- `2026-05-15 11:50:36 +08:00`
  `origin/main` 出现合并提交 `11f75af`，对应 `PR #43` 已 merged。

- `2026-05-15 11:50:50 +08:00`
  Orchestrator 观察到 issue 进入 `Done`。
  之后 terminal cleanup 触发 `before_remove` 并删除 `C-52` workspace。

- `2026-05-15 11:57:57 +08:00` 到 `11:58:50 +08:00`
  同一顶层会话继续尝试在原 workspace 中执行命令，统一失败为 `CreateProcess ... No such file or directory`。
  随后会话明确确认 `/home/ss/data/projects/symphony-workspaces/C-52` 已不存在。

## 这条时间线能证明什么

### 已证实

1. 在已核对的材料里，只抓到一条已坐实的 orchestrator 顶层主会话。
2. `turn #8` 在 cleanup 前已经启动，且主线程在 cleanup 前后仍持续推进会话。
3. issue terminal cleanup 与活跃会话生命周期发生了交叠。
4. cleanup 后错误直接退化成底层路径不存在，而不是语义化 invalidation 错误。

### 仍未证实

1. 不能据此写成“系统层面绝无第二顶层线程”。
2. 也不能据此把事故主因写成“已经证实 duplicate thread”。

当前最稳的写法仍然是：

- terminal cleanup 抢在活跃会话完全失效前删掉了 workspace；
- 旧会话没有得到可读 invalidation 语义；
- 用户因此强烈感知成“像双开”。
